# Спека: строки «На 10 дней» и «Подробно на 10 дней» — компактная температура, осадки в мм, иконка кондиции и стрелка ветра

Дата: 2026-08-04. Статус: утверждена пользователем.

## Контекст

Вкладка «Погода» (задеплоена, коммит `e06585f`) содержит карточки «Сейчас», «По часам», «На 10 дней» и «Подробно на 10 дней». Пользователь хочет несколько изменений:

1. В строках «На 10 дней» вместо «ночью +13° / днём +23°» — компактная `+23°/+13°`, сразу после условия, и добавить осадки в мм.
2. В «Подробно на 10 дней» вместо буквенного названия направления ветра — стрелка (куда дует ветер), плюс иконка кондиции в каждой части суток.
3. Обновить справку.

## Изменение 1 — строки «На 10 дней»

Файл: `template.html`, функция `buildWeather10` (стр. 304–318), CSS `.d10row` (стр. 74).

**Текущая строка:**
```
Пн, 3 авг | [иконка] В основном ясно | 10% | ночью +13° / днём +23°
```

**Новая строка:**
```
Пн, 3 авг | [иконка] В основном ясно | +23°/+13° | 0 мм / 10% | облака 45%
```

Правила:
- Температуры: `temp(tmax)+'/'+temp(tmin)` → `+23°/+13°` (градус у обеих). Источники: `D.daily.temperature_2m_max[di]`, `D.daily.temperature_2m_min[di]`.
- Ячейка осадков (всегда две части, разделитель ` / `):
  - осадки в мм: `fmt(D.daily.precipitation_sum[di])+' мм'`; при `null`/`undefined` — `—` (без «мм»);
  - вероятность: `num(D.daily.precipitation_probability_max[di])+'%'` всегда, включая 0%.
- Ячейка облачности: `облака `+`num(D.daily.cloud_cover_mean[di])+'%'`; при `null`/`undefined` — `облака —`.
- Порядок колонок: день | условие | температуры | осадки/вероятность | облачность. Температуры идут сразу после условия.
- Убирается старый столбец вероятности (он теперь в общей ячейке с осадками).

CSS:
- `.d10row`: `grid-template-columns:100px 1fr auto auto auto`.
- Удалить `.d10pp` и `.d10n` (стили переносятся/не нужны).
- Добавить `.d10t{white-space:nowrap}` и `.d10mm{color:var(--muted)}` (осадки мм). Допускается всё в одной ячейке `d10t`; для облачности — класс `.d10cl`.

## Изменение 2 — «Подробно на 10 дней»

Файл: `template.html`, функция `buildWeatherDetail` (стр. 324–351), CSS `.dpart`/`.pt`/`.pw` (стр. 79–83).

**Текущая часть суток:**
```
день
+21°
3 м/с, северо-западный
750 мм
60%
```

**Новая часть суток (6 строк):**
```
☀️ день
+21°
3 м/с [стрелка]
2 мм / 60%
Влажность 60%
облака 45%
```

Правила:
- Иконка кондиции: `wcode(partVal(day, час, 'weather_code'))` → `[wtext, we]`. Иконка `we` выводится в строке `.pt` перед названием части: `☀️ день`. Название части (`x[0]` из `PARTS`) остаётся.
- Ветер (`.pw`): скорость как сейчас (`num(ws)+' м/с'`), направление — стрелка вместо `rumbFull(wd)`:
  - стрелка «куда дует ветер»: поворот `(wd+180)%360` по часовой (↑ = север), глиф `↑`;
  - разметка: `<span class="pwarrow" title="'+rumbFull(wd)+'" style="transform:rotate('+((wd+180)%360)+'deg)">↑</span>`;
  - полное название направления остаётся в `title` (тултип);
  - при `wd==null` — без стрелки (только скорость, как сейчас с «—»).
- Осадки (`.ppr`, новое, после ветра): `num(partVal(day,час,'precipitation'))+' мм / '+num(partVal(day,час,'precipitation_probability'))+'%'`; при отсутствии данных — `—` / `—%`. Подпись не требуется — просто `2 мм / 60%`.
- Влажность (`.phu`): подпись `Влажность `+`num(partVal(day,час,'relative_humidity_2m'))+'%'`.
- Облачность (`.pcl`, последняя строка, заменяет давление): `облака `+`num(partVal(day,час,'cloud_cover'))+'%'`; при `null` — `облака —`.
- Порядок строк: иконка+название, температура, ветер со стрелкой, осадки мм/вероятность, влажность, облачность.
- Иконка и стрелка не добавляют новых зависимостей/CDN; эмодзи и глиф `↑` из системного шрифта.

CSS:
- `.dpart .pt` — контейнер для иконки+названия (flex или inline-разметка, без нарушения текущего вида).
- Добавить `.pwarrow{display:inline-block;font-size:12px;transform-origin:center}` (вращение применяется инлайн-стилем).
- `.dpart .ppr` — переиспользовать класс (был давлением), содержимое меняется; подписи «Влажность»/«облака» — текстом внутри строки.

## Изменение 3 — справка

Строка 132 (раздел «Погода»): «На 10 дней — день, состояние погоды, дневная и ночная температуры, осадки и вероятность, облачность.»

Строка 133: «Подробно на 10 дней — по частям суток (ночь/утро/день/вечер): иконка погоды, температура, ветер со стрелкой направления, осадки и вероятность, влажность, облачность; внизу — восход/закат и долгота дня.»

## Изменение 4 — данные: `cloud_cover_mean` в дневном прогнозе

Файл: `meteo.py`.

- В `DAILY_VARIABLES` (стр. 33–37) добавить `"cloud_cover_mean"`. `build_payload` (стр. 316–336) уже усредняет числовые дневные переменные по моделям — новая переменная пройдёт автоматически (не в `TIME_DAILY`).
- Тесты: обновить `tests/test_render.py` — в фикстуру `_payload()` добавить `cloud_cover_mean` для обеих моделей (напр. `40.0` и `50.0`), в `test_daily_contains_new_variables` (стр. 78) проверить усреднение (`== [45.0]`).

## Прочее

- Функция `fmt` — существующая (округляет, для `precipitation_sum` даёт `1.2` → `1`; допустимо, т.к. `fmt` уже используется для «Осадки за день»). Если пользователю нужна точность 0.1 мм — использовать `Math.round(x*10)/10`. **Решение по умолчанию: существующий `fmt`.**
- Единственное изменение в `meteo.py` — добавление `cloud_cover_mean` в `DAILY_VARIABLES` (+ тест). Остальное — JS в `template.html`.
- `index.html` регенерируется из `template.html` перед проверками.

## Проверки

1. Регенерация: `F:\Meteo\.venv\Scripts\python.exe meteo.py` → `models=13 hours=240`, в JSON присутствует `daily.cloud_cover_mean`.
2. `pytest tests -m "not integration"` → 53 passed / 2 deselected (после добавления теста облачности — 54/2; уточнить в плане).
3. Headless-пробы (`tools\headless_probe.py`):
   - `rows10=10`; каждая строка содержит `мм`, `%` и `облака`; не содержит `ночью` и `днём`; шаблон `+NN°/+NN°`;
   - detail: `parts=40`, `icons=40` (в каждом `.pt` есть иконка), `arrows=40` (в каждом `.pw` есть стрелка), текст `.pw` не содержит слов из `COMPASS_FULL` (кроме тултипа), в каждой части есть `облака`, `мм /`, `Влажность`;
   - `hours=48`, `now=true`, `switch=ok` — регрессия остальных блоков.
